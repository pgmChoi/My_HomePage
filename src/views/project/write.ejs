<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/nav.css">
    <link rel="stylesheet" href="/css/home.css">
</head>
<body>
    <%- include('../partials/nav', { user: user }) %>
    
    <div class="container">
        <h2><%= title %></h2>
        
        <form action="/project/<%= project ? 'edit/' + project.post_id : 'write' %>" method="POST">
            
            <div class="form-group">
                <label>프로젝트 제목</label>
                <input type="text" name="title" class="form-control" value="<%= project ? project.title : '' %>" required placeholder="예: 나만의 포트폴리오 웹사이트">
            </div>

            <div class="form-row">
                <div class="form-col">
                    <label>기술 스택 (예: Node.js, React)</label>
                    <input type="text" name="tech_stack" class="form-control" value="<%= project && project.tech_stack ? project.tech_stack : '' %>">
                </div>
                <div class="form-col">
                    <label>GitHub 주소</label>
                    <input type="text" name="github_url" class="form-control" value="<%= project && project.github_url ? project.github_url : '' %>">
                </div>
            </div>
            
            <div class="form-group">
                <label>데모 사이트 (선택)</label>
                <input type="text" name="demo_url" class="form-control" value="<%= project && project.demo_url ? project.demo_url : '' %>" placeholder="http://...">
            </div>

            <div class="form-group">
                <label>상세 내용</label>
                <textarea name="content" rows="15" class="form-control form-textarea" required placeholder="프로젝트 설명, 주요 기능, 배운 점 등을 작성하세요."><%= project ? project.content : '' %></textarea>
            </div>

            <div class="text-right">
                <a href="/project" class="button btn-secondary">취소</a>
                <button type="submit" class="button"><%= project ? '수정 완료' : '프로젝트 등록' %></button>
            </div>
        </form>
    </div>
</body>
</html>